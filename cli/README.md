# masks CLI

Install from the Pirandello repository:

```bash
cd /path/to/pirandello/cli
uv tool install .
```

Or editable:

```bash
uv tool install -e .
```

Then run `masks --help`.

Full system design: [../docs/design.md](../docs/design.md).
